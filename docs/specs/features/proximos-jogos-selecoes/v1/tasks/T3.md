# TASK – Detalhamento da Task

## 1. Identificacao
- **ID**: T3
- **Nome da Task**: Repository `MatchRepository` — wrapper sqlc + tipos de domínio `Match`/`MatchTeam`
- **model**: sonnet
- **risk**: low
- **gates**: [qa]   # tipo=crud_handler (thin wrapper sobre sqlc, sem regra de negócio)
- **Status**: Concluído
- **Fase**: Fase 2 — Domínio `match`
- **Dependencias**: T2
- **Criterio de Conclusao**: `repository.MatchRepository` expõe `ListUpcomingMatchesByUser(ctx, userID string, cutoff time.Time) ([]Match, error)`, mapeando as linhas sqlc para os tipos de domínio `Match`/`MatchTeam`; testes de integração (SQLite real via `TestNewDB`) comprovam: ordenação `kickoff` ASC, corte estritamente futuro, dedup de partida com duas favoritas, lista vazia, mapeamento completo dos campos e constraints de schema (CHECK + FK). `make test` verde.

---

## 2. Objetivo da Task
Implementar a camada de acesso a dados do domínio `match`: um thin wrapper sobre a query gerada pelo sqlc (`ListUpcomingMatchesByUser`) que converte as linhas para os tipos de domínio `Match` e `MatchTeam`. Sem regra de negócio (o cálculo do corte vive no service, T4). Espelha `nationalteam/repository`.

---

## 3. Arquivos Impactados

### 3.1 Arquivos a Criar
| Arquivo | Descricao |
|---------|-----------|
| `internal/domain/match/repository/match_repository.go` | `MatchRepository` (concreto) sobre `*sqlc.Queries`; tipos de domínio `Match`/`MatchTeam`; método `ListUpcomingMatchesByUser`; mapper `toMatch` das linhas sqlc. |
| `internal/domain/match/repository/match_repository_test.go` | Teste de integração com `testutil.TestNewDB`: futuro, dedup, ordenação, vazio, mapeamento de campos, CHECK/FK. |

### 3.2 Arquivos a Modificar
| Arquivo | Modificacao |
|---------|------------|
| (nenhum) | — |

### 3.3 Arquivos de Referencia
| Arquivo | Motivo da Consulta |
|---------|-------------------|
| `internal/domain/nationalteam/repository/national_team_repository.go` | Molde do repository concreto (construtor `New...(q *sqlc.Queries)`, tipo de domínio, mapper `toDomain`, `fmt.Errorf("...: %w", err)`). |
| `internal/domain/nationalteam/repository/national_team_repository_test.go` | Molde do teste de integração (`newRepo`, `testutil.TestNewDB`, seed IDs). |
| `internal/infra/db/sqlc/**` | Tipos gerados em T2 (`sqlc.Match`, struct de retorno de `ListUpcomingMatchesByUser`, campos `HomeCode`/`AwayCode`/etc). |
| `internal/testutil/db.go` | `TestNewDB` (SQLite real efêmero migrado + seed). |
| `.claude/rules/grpc-layers.md` | Repository = thin wrapper, sem regra de negócio. |

---

## 4. Detalhes de Implementacao
- [x] Definir os tipos de domínio: `Match{ ID string; Kickoff time.Time; HomeTeam MatchTeam; AwayTeam MatchTeam; Stadium, City, Stage string }` e `MatchTeam{ ID, Name, FlagURL, Code string }`.
- [x] Implementar `NewMatchRepository(q *sqlc.Queries) *MatchRepository`.
- [x] Implementar `ListUpcomingMatchesByUser(ctx, userID string, cutoff time.Time) ([]Match, error)`: chamar a query gerada (passando params `cutoff`/`user_id` conforme nomes gerados pelo sqlc), mapear cada linha com `toMatch`, retornar `[]Match` (slice vazio, não nil, quando não há jogos — `make([]Match, 0, len(rows))`).
- [x] Implementar `toMatch(row)` mapeando os aliases `home_*`/`away_*` da linha para `HomeTeam`/`AwayTeam` (idioma idiomático Go: `FlagURL` no domínio mapeado de `FlagUrl` gerado).
- [x] Sem sentinela nova (a query é multi-linha; ausência de jogos é slice vazio, não erro).
- [x] Wrap de erro com `fmt.Errorf("list upcoming matches by user: %w", err)` no caminho de falha do banco.

---

## 5. Testes

### 5.2 Testes de Integracao

#### Repository: `MatchRepository` (`internal/domain/match/repository/match_repository_test.go`)

Setup: SQLite real efêmero via `testutil.TestNewDB(t)` (migrations + seed + `foreign_keys=ON`); inserir `matches` e favoritas (`user_national_teams`) direto via `*sql.DB`. `cutoff` fixo via `time.Date(...UTC)`. FK via ids do seed (Brasil `...0001`, Argentina `...0002`, Coreia do Sul `...0016`).

| CT | Teste | Objetivo | Fluxo | Validação |
|----|-------|----------|-------|-----------|
| T3-CT-001 | TestIntegration_ListUpcoming_OrdersByKickoffAsc | Verificar que as partidas retornam em ordem cronológica crescente independente da ordem de inserção (CA2) | favorita Brasil; inserir jogo T0+48h e jogo T0+2h → `ListUpcomingMatchesByUser(ctx, user, T0)` | 2 partidas; `matches[0]`=T0+2h, `matches[1]`=T0+48h |
| T3-CT-002 | TestIntegration_ListUpcoming_StrictlyFutureCut | Verificar que só `kickoff > cutoff` retorna (boundary: `== cutoff` excluído) (CA3) | favorita Brasil; jogos em T0-1s, T0, T0+1s → cutoff=T0 | exatamente 1 partida (T0+1s); past e equal ausentes |
| T3-CT-003 | TestIntegration_ListUpcoming_DedupTwoFavorites | Verificar que partida com duas favoritas do mesmo usuário aparece uma única vez (CA4) | favoritas Brasil+Argentina; partida Brasil×Argentina futura | `require.Len(matches, 1)` (sem duplicata) |
| T3-CT-004 | TestIntegration_ListUpcoming_NoFavorites_Empty | (companion negativo) Verificar que usuário sem favoritas → slice vazio, não nil, sem erro (CA5) | usuário sem `user_national_teams` → `ListUpcomingMatchesByUser` | `NoError`; `NotNil`; `len==0` |
| T3-CT-005 | TestIntegration_ListUpcoming_FavoritesNoFutureGames_Empty | (companion negativo) Verificar que favorita só com jogos passados → slice vazio sem erro (CA5) | favorita Brasil; único jogo em T0-1h → cutoff=T0 | `NoError`; `len==0` |
| T3-CT-006 | TestIntegration_ListUpcoming_MapsAllFields | Verificar mapeamento completo de `Match`/`MatchTeam` (id/name/flag_url/code home+away, stadium, city, stage, kickoff) (CA6) | favorita Brasil; partida Brasil×Coreia do Sul com campos conhecidos | todos os campos batem com o inserido e o seed; `HomeTeam.Code=='BRA'`, `AwayTeam.Code=='KOR'`; `Kickoff.UTC()` igual |

### 5.4 Cenarios de Erro

| Cenário | Objetivo | Trigger | Código/Status | Log Esperado |
|---------|----------|---------|---------------|--------------|
| T3-CT-007 — CHECK home≠away (TestIntegration_Matches_RejectsSameTeam) | Verificar que o schema rejeita partida com mandante == visitante | INSERT direto `home_team_id == away_team_id == ...0001` | `require.Error`; 0 linhas inseridas | — |
| T3-CT-008 — FK inexistente (TestIntegration_Matches_RejectsUnknownTeam) | Verificar que `foreign_keys=ON` rejeita FK inexistente | INSERT direto com `away_team_id = 00000000-...` | `require.Error`; integridade preservada | — |
| T3-CT-009 — erro de banco propagado (TestIntegration_ListUpcoming_PropagatesDBError) | Verificar que erro do banco é propagado wrappado, sem panic | `ctx` cancelado antes da chamada → `ListUpcomingMatchesByUser` | `require.Error`; `errors.Is(err, context.Canceled)` (nunca substring) | — |

#### Testes Existentes a Modificar
| Arquivo | Motivo da Modificação |
|---------|----------------------|
| (nenhum) | Novo arquivo `match_repository_test.go`. Este é o **owning layer** do caminho-feliz com dados (o E2E em T7 não semeia partidas). Consultar 000002 para o nome exato da Coreia do Sul no seed. CT-T2-001..005 (schema da migração 000007) podem viver no mesmo arquivo. |

---

## 6. Notas / Observacoes
- O teste de integração precisa **inserir partidas** diretamente no SQLite real (`TestNewDB` retorna `*sql.DB`), pois o seed não traz `matches`. Usar os ids do seed (Brasil `a1f3c5e7-0001-...-001`, etc.) como FK e a relação `user_national_teams` para semear favoritas.
- Cobre o caminho-feliz **com dados** da feature (o E2E, T7, não semeia partidas — decisão deliberada, SCOPE §5).
- CHECK (home≠away) e FK inexistente: testar a rejeição no insert (caminho de erro de schema), validando `foreign_keys=ON` e o CHECK da migração 000007.
- O nome exato dos params/campos gerados pelo sqlc só é conhecido após T2 (`make sqlc`); ajustar o mapper à struct gerada.

---

## 7. Checklist Final
- [x] Implementada conforme Scope
- [x] Testes unitarios criados/atualizados
- [x] Testes de integracao criados/atualizados
- [x] Criterio de conclusao atendido
- [x] Revisada
